spring:
  profile:
    active: {prof:test}
  flyway:
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/cinema?currentSchema=user
    username: postgres
    password: postgres

management:
  endpoints:
    web:
      exposure:
        include: mappings

